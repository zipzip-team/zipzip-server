# 실제 .env.dev는 서버 ~/zipzip-deploy-dev/ 안에만 존재하고 git에는 올리지 않는다.
# 아래는 docker-compose.dev.yml의 ${DOCKERHUB_IMAGE} 변수 치환용 값 예시.
# (SPRING_DATASOURCE_DEV_* 값은 CD가 배포할 때마다 stdin으로 덮어써서 이 파일에 함께 기록한다 —
#  실제 값은 여기 적지 말 것)
DOCKERHUB_IMAGE=<dockerhub-username>/zipzip-be
